/**
 * 1. authenticaiopn: if the user is genuine
 * 2. authorization: if admin/ user/ accesses
 * sign in === login
 * sign up === register
 * ***there are some combination used as well like: sign in & register, sighn up & sign in
 * 
 */